# AI Report Agent — 後端依賴
# 沿用原 AI- 專案的報告引擎,額外加 Web 服務層

fastapi>=0.110
uvicorn[standard]>=0.29

# 報告引擎(與原 AI- 專案相同)
pandas
openpyxl
docxtpl>=0.20.2
python-docx
python-pptx>=1.0.0
pymupdf>=1.27.0
Pillow

# Windows COM(docx/pptx 轉檔、視覺填充需要)
pywin32; platform_system == "Windows"

# AI(選用:Gemini planner / reviewer;Ollama 走 stdlib urllib 免額外套件)
google-genai>=2.0.0
python-dotenv>=1.0.0
